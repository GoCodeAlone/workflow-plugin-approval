# Minimal config exercising workflow-plugin-approval.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml
version: 1
modules:
  - name: approval-provider
    type: approval.engine
    config: {}
workflows:
  pipeline:
    steps:
      - name: example
        type: step.approval_request
        config: {}
